model:
  hidden_dim: 128
  n_layers: 4
  dropout: 0.1

train:
  batch_size: 32
  lr: 0.0003
  weight_decay: 0.00001
  epochs: 60
  patience: 10
  seed: 42

paths:
  splits_file: ../data/splits.json
  experiment_dir: ../experiments/2d_only
